import * as React from 'react';

import Layout from '@/components/layout/Layout';

export default () => {
  const seoTitle = ``;

  return (
    <Layout>
      <main>
        <section className='bg-white'>
          <span>Home Page</span>
        </section>
      </main>
    </Layout>
  );
};
